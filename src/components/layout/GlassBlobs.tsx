/** Fixed, slowly drifting colour blobs behind marketing / account pages (not used in the editor). */
export function GlassBlobs() {
  return (
    <div className="glass-bg-blobs" aria-hidden>
      <div className="glass-blob glass-blob-1" />
      <div className="glass-blob glass-blob-2" />
      <div className="glass-blob glass-blob-3" />
      <div className="glass-blob glass-blob-4" />
    </div>
  )
}
